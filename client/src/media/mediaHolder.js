export const icons = {
    searchbar: require('./icons/searchbar-icon.svg').default,
    dropdown: require('./icons/dropdown-icon.svg').default,
    cancel: require('./icons/cancel-icon.svg').default
}

export const images = {
    defaultProductPic: require('./images/dummy-product-pic.png')
}
